{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AppOptions": {
    "Db": "${{ secrets.APP_JWT_SECRET }}",
    "JwtSecret": "${{ secrets.NEON_CONN_STRING }}"
  }
}
